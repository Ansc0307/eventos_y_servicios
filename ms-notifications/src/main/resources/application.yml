server:
  port: 8083

spring:
  profiles:
    active: openapi

  datasource:
    url: jdbc:postgresql://localhost:5432/notifications_db
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none   # nonoe en producción, o si no quieres perder datos
    show-sql: true       # muestra las queries en consola
