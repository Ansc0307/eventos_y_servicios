# Copia este archivo como .env y ajusta valores seguros
CONFIG_SERVER_USER=config
CONFIG_SERVER_PASSWORD=config
CONFIG_GIT_URI=https://github.com/JoshNisth/eventos-config-repo.git
ENCRYPT_KEY=CHANGE_ME_GENERATE_A_SECURE_RANDOM_HEX
KEYCLOAK_ADMIN_CLIENT_SECRET=CHANGE_ME_KEYCLOAK_CLIENT_SECRET
SSL_KEYSTORE_PASSWORD=
# Agrega aquí otras variables sensibles futuras (tokens, secrets, etc.)

# Keycloak admin service account (ms-usuarios)
# Nota: el resto de variables no son secretas y se setean en docker-compose.yml
# Solo el secret debe venir por .env

# Keycloak SMTP (para emails de verificación / reset password)
# Si KEYCLOAK_SMTP_HOST está vacío, el contenedor keycloak-init NO configura SMTP.
# Gmail: usa App Password (no tu password normal) y 2FA activado.
# Para aplicar cambios sin tumbar todo el stack:
#   docker compose up -d keycloak-init --force-recreate
#   docker logs -f keycloak-init

# Preset recomendado para DEV (Mailpit local en Docker Compose):
#   KEYCLOAK_SMTP_HOST=mailpit
#   KEYCLOAK_SMTP_PORT=1025
#   KEYCLOAK_SMTP_AUTH=false
#   KEYCLOAK_SMTP_STARTTLS=false
#   KEYCLOAK_SMTP_SSL=false
#
# URL pública (desde tu navegador) para que los links de verificación apunten bien.
# Recomendado en dev: http://localhost:8090
# KEYCLOAK_PUBLIC_BASE_URL=http://localhost:8090
KEYCLOAK_SMTP_HOST=
KEYCLOAK_SMTP_PORT=587
KEYCLOAK_SMTP_FROM=
KEYCLOAK_SMTP_FROM_DISPLAY_NAME=Eventos
KEYCLOAK_SMTP_USER=
KEYCLOAK_SMTP_PASSWORD=
KEYCLOAK_SMTP_STARTTLS=true
KEYCLOAK_SMTP_SSL=false
KEYCLOAK_SMTP_AUTH=true
